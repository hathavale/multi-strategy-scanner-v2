services:
  - type: web
    name: multi-strategy-scanner-v2
    runtime: python
    buildCommand: |
      apt-get update && apt-get install -y build-essential gfortran
      pip install -r requirements.txt
    startCommand: gunicorn backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
